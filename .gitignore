# Credentials and API keys (NEVER commit)
.env
.env.local

# Your real personal data (keep private; ship data.example/ instead)
/data/
/config/job_preferences.md

# Generated CVs and pipeline output
/outputs/
/job_offers/
job_search_results_*.json

# LaTeX build artifacts
*.pdf
*.aux
*.log
*.out
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.synctex.gz
*.synctex
*.synctex*(busy)
build/

# Keep example PDFs if you intentionally add any:
!examples/**/*.pdf

# Python
venv/
.venv/
__pycache__/
*.pyc

# IDE / OS
.DS_Store

# new-since-last-run state
!state/seen_jobs.json
